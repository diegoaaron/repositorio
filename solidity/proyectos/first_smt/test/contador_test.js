const { expect } = require("chai");
const { ethers } = require("hardhat");

describe("Greeter", function() {
    it("Definimos el valor inicial del contador", async function() {
        const Contador = await ethers.
    })
})